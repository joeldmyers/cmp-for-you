$(document).ready(function () {
    $('#singlebutton').click(function (e) {
        e.preventDefault();

        $.ajax({
            type: 'POST',
            url: 'util/post.php',
            data: $("#patfrm").serialize(),
            success: function (d) {
                $(".patatient-signup").html(d);
            }

        });
    });

});
///////////// Validation Script /////////////////////
$(function () {

    $("#loginform").validate({
        rules: {
            email: {
                required: true,
                email: true
            },
            password: "required",
        },
        submitHandler: function (form) {
            form.submit();
        }
    });

});



//////////////// File Upload ////////////////////
$(function () {
    $("#fileupload").change(function () {
        if (typeof (FileReader) != "undefined") {
            var dvPreview = $("#dvPreview");
            dvPreview.html("");
            var regex = /^([a-zA-Z0-9\s_\\.\-:])+(.jpg|.jpeg|.gif|.png|.bmp)$/;
            $($(this)[0].files).each(function () {
                var file = $(this);
                if (regex.test(file[0].name.toLowerCase())) {
                    var reader = new FileReader();
                    reader.onload = function (e) {
                        var img = $("<img />");
                        img.attr("style", "height:100px;width: auto");
                        img.attr("src", e.target.result);
                        dvPreview.append(img);
                    }
                    reader.readAsDataURL(file[0]);
                } else {
                    alert(file[0].name + " is not a valid image file.");
                    dvPreview.html("");
                    return false;
                }
            });
        } else {
            alert("This browser does not support HTML5 FileReader.");
        }
    });
});




$(function () {

    var t = $("#patfrm").validate({
        rules: {
            fname: "required",
            lname: "required",
            gender: "required",
            pseudoname: "required",
            email: {
                required: true,
                email: true
            },
            username: "required",
            password: {
                required: true,
            },
            repassword: {
                equalTo: "#password",
    }

        },
    });

});


$(function () {

    var t = $("#docfrm").validate({
        rules: {
            fname: "required",
            lname: "required",
			zipcode: "required",
			city: "required",
            gender: "required",
            pseudoname: "required",
            email: {
                required: true,
                email: true
            },
            username: "required",
            password: {
                required: true,
            },
            repassword: {
                equalTo: "#password",
    }

        },
    });

});

$(function () {
	
	
    var t = $("#docfrm_update").validate({
	
        rules: {
            fname: "required",
            lname: "required",
			zipcode: "required",
			city: "required",
            gender: "required",
            pseudoname: "required",
            email: {
                required: true,
                email: true
            },
            username: "required",
            password1: {
                required: true,
            },
            repassword1: {
                equalTo: "#password1",
    }

        },
    });

});

//$(document).ready(function () {
//    $('#submit').click(function (e) {
//        e.preventDefault();
//
//        $.ajax({
//            type: 'POST',
//            url: 'checkemail.php',
//            data: $("#patfrm").serialize(),
//            success: function (data) {
//               $(".signup-form").html(data);
//            }
//
//        });
//   });
//
//});